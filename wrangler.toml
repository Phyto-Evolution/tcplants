name = "plantking"
main = "cloudflare-worker.js"
compatibility_date = "2024-09-23"

[[d1_databases]]
binding = "IOT_DB"
database_name = "tcplants-iot"
database_id = "ecae7634-02b6-4f12-b283-6f4f950c2cdf"

# Secrets — stored in Cloudflare dashboard, never committed here
# Workers → plantking → Settings → Variables and Secrets
# [vars] below is for non-secret config only

[vars]
# Non-secret vars (safe to commit)
# SECRET vars set in dashboard: GROQ_KEY, CLAUDE_KEY, GEMINI_KEY, IOT_KEY, BRAVE_KEY
